service: worker
runtime: nodejs20
manual_scaling:
  instances: 1
instance_class: B2
# The entrypoint is the command to starts worker script.
# App Engine will run this command instead of the default 'npm start'.
entrypoint: node workers/worker.js

includes:
  - worker-prod-env.yaml

